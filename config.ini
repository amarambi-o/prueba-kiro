; ============================================================
; Bank Modernization — Configuracion por banco
; Para agregar un banco nuevo, copia el bloque y descomenta
; ============================================================

; Banco activo: BankDemo (NTT DATA demo)
name    = BankDemo
server  = (local)
db      = demo
bucket  = bank-modernization-kiro
prefix  = bankdemo
region  = eu-central-1
dtsx    =
tablas  = payments_raw, customers_dim, transfers_raw

; ── Banco 2 (ejemplo futuro) ─────────────────────────────
; name    = BancoXYZ
; server  = sqlserver.bancoxyz.com
; db      = core_banking
; bucket  = bancoxyz-modernization
; prefix  = bancoxyz
; region  = us-east-1
; dtsx    = etl/bancoxyz_etl.dtsx
; tablas  = transacciones, clientes, cuentas
